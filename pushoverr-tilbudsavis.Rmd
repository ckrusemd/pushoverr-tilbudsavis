---
title: "Tilbudsavis - ChatGPT Prompts"
author: "Christian Kruse"
date: "`r Sys.Date()`"
output: 
  bookdown::gitbook:
    css: style.css
---

```{r include=FALSE}
knitr::opts_chunk$set(echo = FALSE,message = FALSE,warning = FALSE)
options(scipen=999)
```

# Tilbudsavis

```{r}

if (!require(pacman)) { install.packages("pacman") }
pacman::p_load(pushoverr,
               dplyr,
               tidyr,
               lubridate,
               scales,
               httr,
               ggplot2,
               rPref,
               jsonlite,
               DT,
               glue)

```


```{r warning=FALSE,message=FALSE,include=FALSE}
readRenviron(path = "Renviron.site")
```

```{r warning=FALSE,message=FALSE,include=FALSE}
pushoverr::set_pushover_app(token = Sys.getenv("PUSHOVER_APPKEY"))
pushoverr::set_pushover_user(user = Sys.getenv("PUSHOVER_USERKEY"))
```

```{r}

keywords = c("skyr",
             "yoghurt",
             "bilka",
             "rema",
             "coop",
             "netto",
             "meny",
             "tun",
             "cola",
             "nyborg",
             "pepsi",
             "oksekød",
             "pizza",
             "rugbrød",
             "slik",
             "pølser",
             "lego",
             "lotus",
             "vitamin",
             "legetøj",
             "switch",
             "playstation",
             "tabs",
             "ps5",
             "starbucks",
             "chokolade",
             "opbevaring",
             "tandpasta",
             "neutral",
             "slik",
             "kaffe",
             "laks",
             "neutral",
             "vitamin",
             "burger"
             )

df_etilbudsavisen = do.call("rbind",lapply(keywords,function (keyword) {
  message(keyword)
  tryCatch(expr = {
  url = glue("https://etilbudsavis.dk/api/squid/v2/offers/search?query={keyword}&r_lat=55.7715601&r_lng=12.4943525&r_radius=30000&r_locale=da_DK&limit=100&offset=0")
  api_url = utils::URLencode(url)
  api_result = httr::GET(api_url)
  api_json = rawToChar(api_result$content)
  result = jsonlite::fromJSON(api_json)
  
  images_ = data.frame(images=result$images$zoom)
  
  temp_df = result %>% 
    dplyr::select(heading,description,run_from,run_till,pricing) %>% 
    unnest_wider(pricing) %>% 
    dplyr::select(-pre_price,-currency) %>% 
    bind_cols(images_) %>% 
    dplyr::mutate(images=paste0("<img src='",images,"' height='200' width='200'></img>"))
  
  dealer_dictionary = result$dealer %>% dplyr::select(name) 
  quantity_dictionary_unit = result$quantity$unit$symbol
  quantity_dictionary_size = result$quantity$size$to
  
  temp_df %>% 
    bind_cols(dealer_dictionary) %>% 
    dplyr::mutate(size=quantity_dictionary_size,
                  unit=quantity_dictionary_unit) %>% 
    dplyr::mutate(run_from=ymd(substr(run_from,start = 1,stop = 10))) %>% 
    dplyr::mutate(run_till=ymd(substr(run_till,start = 1,stop = 10))) %>% 
    filter(run_from<=Sys.Date()) %>% 
    filter(run_till>=Sys.Date()) %>% 
    # filter(!name %in% c("Harald Nyborg","Kop & Kande","Elgiganten","Lidl","Min Købmand","Imerco","Skousen","LET-KØB")) %>% 
    dplyr::mutate(name_full=name) %>% 
    dplyr::mutate(name=recode(name,
                              "føtex"="F",
                              "Netto"="N",
                              "Irma"="I",
                              "Coop365"="C",
                              "MENY"="M",
                              "SuperBrugsen"="SB",
                              "Kvickly"="K",
                              "Bilka"="B",
                              "REMA 1000"="R",
                              "Harald Nyborg"="HN",
                              "Dagli'Brugsen"="DB",
                              "SPAR"="S"),
                  keyword=keyword)
    
  },error=function(e) {
    data.frame()
  })
})) %>% 
  distinct() %>% 
  # dplyr::select(-run_from,-run_till) %>%  
  dplyr::mutate(size_converted=case_when(unit=="g"~size*1,
                                         unit=="kg"~size*1000,
                                         unit=="l"~size*1000,
                                         unit=="cl"~size*10,
                                         unit=="ml"~size*1,
                                         TRUE~as.numeric(NA))) %>% 
  dplyr::mutate(price_per_kg=price/(size_converted/1000)) %>% 
  dplyr::mutate(price_per_kg=round(price_per_kg,1)) %>% 
  unite(size,size,unit,sep = " ") 
  
  df_etilbudsavisen
```

## All

```{r}
df_etilbudsavisen %>% DT::datatable(.,filter="top",escape=FALSE)
```

```{r}
openxlsx::write.xlsx(x = df_etilbudsavisen,file = "_book/df_etilbudsavisen.xlsx")
```

## Centers

### Bilka

```{r}
df_etilbudsavisen %>% filter(keyword=="bilka") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Netto

```{r}
df_etilbudsavisen %>% filter(keyword=="netto") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Rema 1000

```{r}
df_etilbudsavisen %>% filter(keyword=="rema") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Coop365

```{r}
df_etilbudsavisen %>% filter(keyword=="coop") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Meny

```{r}
df_etilbudsavisen %>% filter(keyword=="meny") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Harald Nyborg

```{r}
df_etilbudsavisen %>% filter(keyword=="nyborg") %>% arrange(heading) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

## By subject

### Skyr

```{r}
df_etilbudsavisen %>% filter(keyword=="skyr") %>% arrange(price_per_kg) %>%   DT::datatable(.,filter="top",escape=FALSE)
```

### Oksekød

```{r}
df_etilbudsavisen %>% filter(keyword=="oksekød") %>% arrange(price_per_kg) %>%  DT::datatable(.,filter="top",escape=FALSE)
```
### Neutral

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("neutral")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```
### Tabs

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("tabs")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Tun

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("tun")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Slik

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("slik")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Legetøj

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("lego","switch","playstation","ps5")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Sodavand

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("cola","pepsi","sodavand")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Kaffe

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("kaffe","starbucks")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Vitaminer

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("kaffe","starbucks")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Burger

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("burger")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

### Pølser

```{r}
df_etilbudsavisen %>% filter(keyword %in% c("pølser")) %>%  arrange(price_per_kg) %>% DT::datatable(.,filter="top",escape=FALSE)
```

## Pushoverr

```{r}
pushoverr_string = df_etilbudsavisen %>% 
  filter(keyword %in% c("skyr","tun","sodavand","pepsi","cola"),
         name_full %in% c("Coop365","Kvickly","Meny","Netto","føtex","Rema 1000","Bilka")) %>% 
  dplyr::select(name,heading,price,size) %>% 
  distinct() %>% 
  group_by(name) %>% 
  dplyr::mutate(label_=paste0(heading,", ",size,"/",scales::number(price,1))) %>% 
  dplyr::select(name,label_) %>% 
  dplyr::mutate(text=paste(label_,collapse="\n")) %>% 
  dplyr::select(name,text) %>% 
  distinct() %>% 
  ungroup() %>% 
  unite(name,name,text,sep = ":\n") %>% 
  pull(name) %>% 
  paste0(.,collapse="\n")
# nchar(pushoverr_string)
# cat(pushoverr_string)
# message(nchar(pushoverr_string))
pushoverr::pushover(pushoverr_string)
```

